@model IEnumerable<MvcCore.Models.Employee>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Employee List";
}
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title></title>
    <link href="~/css/TestCss.css" rel="stylesheet" />
</head>
<body>
    @*<table id="table1">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Department</th>
            </tr>
            @foreach (var employee in Model)
            {
                <tr>
                    <td>@employee.Id</td>
                    <td>@employee.Name</td>
                    <td>@employee.Department</td>
                </tr>
            }
        </table>*@

    @*<br />
        <br />
        <button onclick="myFunction()">Hide me</button>*@
        
    <div class="mx-auto">
        @foreach (var employee in Model)
        {
            <div class="card m-3" style="width:400px">
                <img class="card-img-top" src="img_avatar1.png" alt="Card image - yet to add">
                <div class="card-body">
                    <h4 class="card-title">@employee.Name</h4>

                    @*<p class="card-text">Some example text.</p>*@
                    <a href="/Home/Details/@employee.Id" class="btn btn-primary m-1">View</a>
                    <a href="#" class="btn btn-primary m-1">Edit</a>
                    <a href="#" class="btn btn-primary m-1">Delete</a>
                </div>
            </div>
        }
    </div>
    <script src="~/js/TestJs.js"></script>
</body>
</html>